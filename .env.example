# Leave empty to keep the existing embedded SQLite demo backend.
DISPATCH_AGENT_API_URL=
DISPATCH_AGENT_API_TOKEN=

# Used by the independently deployed FastAPI agent service.
AGENT_SERVICE_DB_PATH=/tmp/dispatch_agent_service.db
AGENT_SERVICE_TOKEN=
